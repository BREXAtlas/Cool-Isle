# Cool Isle is a secret-free public-data mirror. Its WeatherChart panel reads
# only the generated public data published at:
# https://brexatlas.github.io/WeatherChartUK/

# Do not add MET_OFFICE_API_KEY or social-provider credentials to Cool Isle.
# Those server-side values belong only in BREXAtlas/WeatherChartUK as GitHub
# Actions repository secrets. They must never be exposed to browser JavaScript,
# generated JSON, logs, URLs, screenshots, or the Pages artifact.
